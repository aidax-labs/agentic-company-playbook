# [Project Name] — Context for agents

## What this is
[One paragraph describing the product and its purpose]

## Tech stack
- Language: 
- Framework:
- Database:
- Deployment target:
- Testing:

## Key directories
- `src/` — [description]
- `docs/adr/` — Architecture Decision Records
- `docs/prds/` — Product Requirements Documents
- `openapi/` — OpenAPI specs

## Running locally
```bash
# Install dependencies
[command]

# Set up environment
cp .env.example .env
# Edit .env with your local values

# Start the dev server
[command]
```

## Running tests
```bash
# Unit tests
[command]

# Integration tests
[command]

# Type check
[command]
```

## Mock server
The Microcks mock server is available at: [URL or "run locally with docker-compose up"]

## Key conventions
- [Naming convention — e.g., "snake_case for DB columns, camelCase in code"]
- [File organization — e.g., "one file per domain in src/routes/"]
- [Error handling — e.g., "all errors use the Errors helper from src/lib/response.ts"]
- [Testing — e.g., "integration tests hit a real test database, no mocks"]

## Do not modify
- `[path]` — [reason]
- `[path]` — [reason]

## Open questions / known issues
- [Any known quirks agents should be aware of]
